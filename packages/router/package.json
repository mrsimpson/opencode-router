{
  "name": "@opencode-ai/router",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outdir=dist --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && tsc --noEmit && mkdir -p dist/scripts && cp src/scripts/capability-routing.sh dist/scripts/capability-routing.sh",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx --env-file=.env.local --watch src/index.ts",
    "test": "vitest run src/"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.0.0",
    "http-proxy": "^1.18.1",
    "human-id": "4.1.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.0",
    "@types/http-proxy": "^1.17.16",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
